\documentclass[12pt,a4paper]{report}

% ─── Packages ───────────────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{array}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{tocloft}
\usepackage{setspace}
\usepackage{parskip}
\usepackage{mdframed}
\usepackage{fontawesome5}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{enumitem}
\usepackage{lmodern}

% ─── Page Geometry ──────────────────────────────────────────────────────────
\geometry{
  top=2.5cm,
  bottom=2.5cm,
  left=3cm,
  right=2.5cm
}

% ─── Colors ─────────────────────────────────────────────────────────────────
\definecolor{primaryblue}{RGB}{0, 51, 102}
\definecolor{accentred}{RGB}{180, 0, 0}
\definecolor{lightgray}{RGB}{245, 245, 245}
\definecolor{codegray}{RGB}{240, 240, 240}
\definecolor{darkgray}{RGB}{80, 80, 80}
\definecolor{codegreen}{RGB}{0, 120, 0}
\definecolor{codepurple}{RGB}{120, 0, 120}
\definecolor{warnorange}{RGB}{204, 102, 0}

% ─── Hyperref Setup ─────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=primaryblue,
  urlcolor=primaryblue,
  citecolor=primaryblue,
  pdftitle={DevSecOps Web Security Project Report},
  pdfauthor={Mossaab Saouti}
}

% ─── Header / Footer ────────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textcolor{darkgray}{DevSecOps / Web Security}}
\fancyhead[R]{\small\textcolor{darkgray}{Mossaab Saouti}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ─── Section Title Styling ──────────────────────────────────────────────────
\titleformat{\chapter}[block]
  {\normalfont\Large\bfseries\color{primaryblue}}
  {\thechapter.}{10pt}{}
  [\vspace{2pt}\titlerule]

\titleformat{\section}
  {\normalfont\large\bfseries\color{primaryblue}}
  {\thesection}{8pt}{}

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\color{darkgray}}
  {\thesubsection}{6pt}{}

% ─── Code Listing Style ─────────────────────────────────────────────────────
\lstdefinestyle{bashstyle}{
  backgroundcolor=\color{codegray},
  basicstyle=\ttfamily\small,
  breaklines=true,
  captionpos=b,
  commentstyle=\color{codegreen},
  keywordstyle=\color{primaryblue}\bfseries,
  stringstyle=\color{codepurple},
  numberstyle=\tiny\color{darkgray},
  numbers=left,
  numbersep=8pt,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{darkgray},
  tabsize=2,
  showstringspaces=false,
  language=bash
}

\lstdefinestyle{pythonstyle}{
  backgroundcolor=\color{codegray},
  basicstyle=\ttfamily\small,
  breaklines=true,
  captionpos=b,
  commentstyle=\color{codegreen}\itshape,
  keywordstyle=\color{primaryblue}\bfseries,
  stringstyle=\color{codepurple},
  numberstyle=\tiny\color{darkgray},
  numbers=left,
  numbersep=8pt,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{darkgray},
  tabsize=2,
  showstringspaces=false,
  language=Python
}

\lstdefinestyle{urlstyle}{
  backgroundcolor=\color{codegray},
  basicstyle=\ttfamily\small\color{accentred},
  breaklines=true,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{darkgray},
  showstringspaces=false
}

% ─── Custom Boxes ───────────────────────────────────────────────────────────
\newmdenv[
  backgroundcolor=lightgray,
  linecolor=primaryblue,
  linewidth=2pt,
  leftline=true,
  rightline=false,
  topline=false,
  bottomline=false,
  innerleftmargin=10pt,
  innerrightmargin=10pt,
  innertopmargin=8pt,
  innerbottommargin=8pt
]{infobox}

\newmdenv[
  backgroundcolor=red!5,
  linecolor=accentred,
  linewidth=2pt,
  leftline=true,
  rightline=false,
  topline=false,
  bottomline=false,
  innerleftmargin=10pt,
  innerrightmargin=10pt,
  innertopmargin=8pt,
  innerbottommargin=8pt
]{warningbox}

% ─── TOC Styling ────────────────────────────────────────────────────────────
\renewcommand{\cftchapfont}{\bfseries\color{primaryblue}}
\renewcommand{\cftsecfont}{\color{darkgray}}
\setlength{\cftbeforechapskip}{6pt}

% ════════════════════════════════════════════════════════════════════════════
%  DOCUMENT
% ════════════════════════════════════════════════════════════════════════════
\begin{document}

% ─── Cover Page ─────────────────────────────────────────────────────────────
\begin{titlepage}
  \centering
  \thispagestyle{empty}

  % University logos — replace the filenames with your actual logo files
  % Place logo files in the same folder as this .tex file
  \begin{figure}[H]
    \centering
    \includegraphics[height=2.5cm]{logo/logo_univ.PNG}
    \hspace{2cm}
    \includegraphics[height=2.5cm]{logo/logo_ensa.PNG}
    % \textcolor{darkgray}{\textit{[Place university logos here]}}
  \end{figure}

  \vspace{1cm}
  \textcolor{darkgray}{\rule{\linewidth}{0.5pt}}
  \vspace{0.5cm}

  {\Huge\bfseries\color{primaryblue} Project Report}\\[0.4cm]
  {\Large\color{darkgray} DevSecOps / Web Security}\\[0.2cm]

  \vspace{0.5cm}
  \textcolor{darkgray}{\rule{\linewidth}{0.5pt}}
  \vspace{1.5cm}

  {\large\bfseries By:}\\[0.3cm]
  {\LARGE\bfseries\color{primaryblue} Mossaab Saouti}\\[0.2cm]
  {\normalsize Cybersecurity \& Threat Intelligence}\\
  {\normalsize National School of Applied Sciences of Tangier}\\[1.5cm]

  {\large\bfseries Supervised by:}\\[0.3cm]
  {\Large\bfseries Said Bouchkaren}\\[2cm]

  \vfill

  {\normalsize Academic Year: 2025/2026}\\
  {\normalsize May 2026}
\end{titlepage}

% ─── Abstract ───────────────────────────────────────────────────────────────
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}
\thispagestyle{fancy}

This report presents the practical work carried out as part of the DevSecOps and Web Security lab at the National School of Applied Sciences of Tangier. The objective was to build, exploit, and secure a Flask web application while integrating automated security testing into a CI/CD pipeline.

Part 1 covered environment setup, STRIDE threat modeling, and hands-on exploitation of four critical vulnerabilities: SQL Injection, Cross-Site Scripting (XSS), OS Command Injection, and Path Traversal. Each vulnerability was analyzed, exploited against an intentionally insecure application, and subsequently remediated in a secure implementation.

Part 2 covers automated security testing using SAST (Bandit), SCA (Safety), and DAST (OWASP ZAP) tools integrated into a GitHub Actions pipeline with security gates. Part 3 addresses Docker container hardening following CIS Benchmark guidelines, runtime WAF monitoring, security headers, and an incident response exercise.

The complete lab demonstrates a full DevSecOps workflow from threat modeling to production hardening, reflecting industry best practices for secure software delivery.

\vspace{1cm}
\textbf{Keywords:} DevSecOps, Web Security, OWASP Top 10, Flask, STRIDE, SAST, DAST, Docker, CI/CD, WAF

% ─── Table of Contents ──────────────────────────────────────────────────────
\tableofcontents
\thispagestyle{fancy}

% ════════════════════════════════════════════════════════════════════════════
\chapter{Introduction}
% ════════════════════════════════════════════════════════════════════════════

\section{Context and Objectives}

Modern software development requires security to be integrated at every stage of the development lifecycle, not added as an afterthought. This approach, known as \textbf{DevSecOps} (Development, Security, and Operations), shifts security left by embedding automated security checks directly into the CI/CD pipeline.

This project was carried out as part of the Web Security and DevSecOps lab at the National School of Applied Sciences of Tangier. The objective was to gain hands-on experience with real-world security techniques by building, breaking, and securing a Flask web application.

The lab covers the full DevSecOps workflow: from identifying threats using structured threat modeling, to exploiting and remediating OWASP Top 10 vulnerabilities, to automating security testing in a CI/CD pipeline, and finally hardening the deployment environment against runtime attacks.

\section{Lab Structure Overview}

The lab is divided into three parts:

\begin{itemize}[leftmargin=1.5cm]
  \item \textbf{Part 1 — Build \& Break:} Environment setup, STRIDE threat modeling, and hands-on exploitation of four critical vulnerabilities in an intentionally insecure Flask application, followed by their remediation.
  \item \textbf{Part 2 — Test \& Automate:} Integration of automated security tools (Bandit, Safety, pytest, OWASP ZAP) into a GitHub Actions CI/CD pipeline with security gates that block insecure code from being merged.
  \item \textbf{Part 3 — Harden \& Monitor:} Docker container hardening following CIS Benchmark guidelines, runtime WAF and rate limiting middleware, security headers implementation, and an incident response exercise.
\end{itemize}

% ════════════════════════════════════════════════════════════════════════════
\chapter{Environment Setup}
% ════════════════════════════════════════════════════════════════════════════

\section{Prerequisites and Tools}

The following tools were required and installed before beginning the lab:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{>{\bfseries}l l X}
\toprule
Tool & Version & Purpose \\
\midrule
Python & 3.11+ & Application runtime and scripting \\
Flask & 2.x & Web application framework \\
Docker & Latest & Containerization platform \\
Git & Latest & Version control \\
WSL (Ubuntu) & 2.0 & Linux environment on Windows \\
VS Code & Latest & Code editor with LaTeX Workshop \\
curl/ Postman & Latest & HTTP request testing \\
\bottomrule
\end{tabularx}
\caption{Tools used in the lab}
\end{table}

\section{Tool Installation}
\textbf{Python:} Installed from the official website. Added to PATH for command line access.
\textbf{Flask:} Installed via pip in a virtual environment.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/flask_version.png}
  \caption{Installed tools versions verified in terminal}
\end{figure}
\textbf{git:} Installed from the official website. Configured with user name and email.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/git_setup.png}
  \caption{Configuration of Git in Terminal}
\end{figure}
\textbf{vscode:} Installed from the official website. \n
\textbf{postman:} Installed from the official website for API testing.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.45\textwidth]{pictures/vsCode.png}
  \hfill
  \includegraphics[width=0.45\textwidth]{pictures/postman.png}
  \caption{VS Code and Postman — development and testing environment}
\end{figure}
\textbf{Docker:} Installed from the official website. WSL integration enabled for Linux containers.
\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/docker.png}
  \caption{Docker Desktop installed}
\end{figure}



\section{Repository Setup}

The lab repository was cloned from GitHub and dependencies were installed inside a Python virtual environment to ensure isolation from the system Python installation:

\begin{lstlisting}[style=bashstyle, caption={Repository setup commands}]
git clone https://github.com/said-dev-ops/dev-sec-ops-lab-1.git
cd dev-sec-ops-lab-1
python -m venv .venv
source .venv/bin/activate   
pip install -r requirements.txt
\end{lstlisting}

% Screenshot placeholder
\begin{figure}[H]
  \centering
  \includegraphics[width=0.45\textwidth]{pictures/cloneRepo.png}
  \hfill
  \includegraphics[width=0.45\textwidth]{pictures/change_origin.png}
  \caption{Cloning the lab repository and changing Git origin to personal fork}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/setupVenv.png}
  \caption{Setting up virtual environment and installing dependencies successfully}
\end{figure}

\section{Windows vs WSL Context}

Initial setup was performed on Windows. However, the lab exercises involving Linux-specific commands (such as \texttt{cat /etc/passwd} and \texttt{ls /home}) required a Linux environment. The environment was therefore migrated to \textbf{WSL (Windows Subsystem for Linux)} running Ubuntu, which provided a realistic Linux server context while maintaining the Windows host machine. All subsequent exploitation exercises were performed inside WSL.

\begin{infobox}
\textbf{Note:} On Windows, the ping command uses \texttt{-n} instead of \texttt{-c}, and system files like \texttt{/etc/passwd} do not exist. WSL was used to replicate a realistic Linux production server environment, as intended by the lab design.
\end{infobox}

\section{Project Structure}

Following the lab instructions, the repository files were organized into three directories corresponding to the three lab parts:

\begin{lstlisting}[style=bashstyle, caption={Project directory structure}]
dev-sec-ops-lab-1/
|-- part1/
|   |-- app_vulnerable.py
|   |-- app_secure.py
|   `-- stride_threat_model.json
|-- part2/
|   |-- sast_scan.py
|   |-- test_security.py
|   `-- devsecops-pipeline.yml
`-- part3/
    |-- security_monitoring.py
    |-- Dockerfile
    `-- docker-compose.yml
\end{lstlisting}

% Screenshot placeholder
\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/tree.png}
  \caption{Organized project directory structure}
\end{figure}

% ════════════════════════════════════════════════════════════════════════════
\chapter{Threat Modeling}
% ════════════════════════════════════════════════════════════════════════════

\section{STRIDE Methodology}

Threat modeling is a structured approach to identifying security risks before writing a single line of code. \textbf{STRIDE} is a mnemonic developed by Microsoft that categorizes threats into six categories:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{>{\bfseries}l l l l}
\toprule
Category & Full Name & Example Threat & Control \\
\midrule
S & Spoofing & SQL injection to bypass login & Parameterized queries \\
T & Tampering & Malicious pickle deserialization & Input validation \\
R & Repudiation & No logs — cannot trace attacker & Audit logging \\
I & Info Disclosure & API returns plaintext passwords & Access control \\
D & Denial of Service & Command injection crashes server & Rate limiting, WAF \\
E & Elevation of Privilege & Path traversal reads /etc/passwd & Least privilege, safe\_join \\
\bottomrule
\end{tabularx}
\caption{STRIDE threat categories}
\end{table}

\section{Threat Modeling Process}

The threat modeling process followed these steps:

\begin{enumerate}[leftmargin=1.5cm]
  \item \textbf{Define scope} — identify assets and trust boundaries
  \item \textbf{Create a Data Flow Diagram (DFD)} — Browser $\rightarrow$ Flask $\rightarrow$ SQLite $\rightarrow$ OS
  \item \textbf{Enumerate threats} using STRIDE for each data flow
  \item \textbf{Score each threat} using DREAD or CVSS
  \item \textbf{Prioritize and assign mitigations}
  \item \textbf{Validate mitigations} and retest
\end{enumerate}

\section{Threat Register — Full Risk Assessment}

The following table maps all identified threats to STRIDE categories, attack vectors, and mitigations:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{>{\bfseries}l X l X l}
\toprule
ID & Threat & STRIDE & Mitigation & Status \\
\midrule
T1 & SQL Injection Login Bypass & Spoofing & Parameterized queries & \textcolor{codegreen}{\textbf{Fixed}} \\
T2 & Pickle Deserialization RCE & Tampering & Use JSON, never pickle & \textcolor{codegreen}{\textbf{Fixed}} \\
T3 & No Audit Logging & Repudiation & Log auth + admin actions & \textcolor{codegreen}{\textbf{Fixed}} \\
T4 & Password Exposure via API & Info Disclosure & Field filtering + auth & \textcolor{codegreen}{\textbf{Fixed}} \\
T5 & OS Command Injection & Denial of Service & Arg list, no shell=True & \textcolor{codegreen}{\textbf{Fixed}} \\
T6 & Path Traversal File Read & Elevation of Priv. & safe\_join + whitelist & \textcolor{codegreen}{\textbf{Fixed}} \\
T7 & Reflected XSS & Info Disclosure & Jinja2 auto-escape + CSP & \textcolor{codegreen}{\textbf{Fixed}} \\
T8 & Hardcoded Secret Key & Spoofing & Load from env variable & \textcolor{codegreen}{\textbf{Fixed}} \\
\bottomrule
\end{tabularx}
\caption{Complete threat register}
\end{table}

% Screenshot placeholder
\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/threat_model.png}
  \caption{STRIDE threat model JSON file}
\end{figure}

% ════════════════════════════════════════════════════════════════════════════
\chapter{Part 1 — Build \& Break}
% ════════════════════════════════════════════════════════════════════════════

% ────────────────────────────────────────────────────────────────────────────
\section{Vulnerability Exploitation}
% ────────────────────────────────────────────────────────────────────────────

The vulnerable application was launched using:

\begin{lstlisting}[style=bashstyle]
cd part1
python app_vulnerable.py
\end{lstlisting}

% Screenshot placeholder
\begin{figure}[H]
  \centering
  \includegraphics[width=0.45\textwidth]{pictures/run_tem.png}
  \hfill
  \includegraphics[width=0.45\textwidth]{pictures/run_brows.png}
  \caption{Vulnerable Flask application running}
\end{figure}

% ────────────────────────────────────────────────────────────────────────────
\subsection{Exercise 1 — SQL Injection (T1)}
% ────────────────────────────────────────────────────────────────────────────

\textbf{Attack Description}

SQL Injection occurs when user-supplied input is directly concatenated into an SQL query without sanitization. An attacker can manipulate the query logic to bypass authentication, extract data, or modify the database.

\textbf{Target:} \texttt{POST /login}

\textbf{Exploitation}

The following payload was entered in the username field on the login page:

\begin{lstlisting}[style=urlstyle, caption={SQL Injection payload}]
admin' OR '1'='1
\end{lstlisting}

The injected query becomes:
\begin{lstlisting}[style=pythonstyle, caption={Resulting malicious SQL query}]
SELECT * FROM users WHERE username='admin' OR '1'='1'
\end{lstlisting}

Since \texttt{'1'='1'} is always true, the query returns the admin creedentials, completely bypassing authentication.

% Screenshot placeholders
\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/login.png}
  \caption{Login page — target of SQL injection attack}
\end{figure}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/dashboard.png}
  \caption{Authentication bypassed using SQL injection}
\end{figure}

\textbf{Impact Analysis}

\begin{warningbox}
\textbf{STRIDE Category:} Spoofing (T1) \\
\textbf{CVSS Score:} 9.8 Critical \\
\textbf{Impact:} An attacker can authenticate as any user, including administrators, without knowing any password. This grants full unauthorized access to the application.
\end{warningbox}

% ────────────────────────────────────────────────────────────────────────────
\subsection{Exercise 2 — Cross-Site Scripting (XSS) (T7)}
% ────────────────────────────────────────────────────────────────────────────

\textbf{Attack Description}

Reflected XSS occurs when user input is included in the HTTP response without proper escaping. An attacker can inject malicious JavaScript that executes in the victim's browser, potentially stealing session tokens or performing actions on their behalf.

\textbf{Target:} \texttt{GET /search?q=}

\textbf{Exploitation}

The following payloads were tested:

\begin{lstlisting}[style=urlstyle, caption={XSS Payload 1 — script tag}]
http://127.0.0.1:5000/search?q=<script>alert(document.cookie)</script>
\end{lstlisting}

\begin{lstlisting}[style=urlstyle, caption={XSS Payload 2 — image error event}]
http://127.0.0.1:5000/search?q=<img src=x onerror=alert(1)>
\end{lstlisting}

Both payloads triggered a JavaScript alert in the browser. The \texttt{document.cookie} payload returned an empty value because Flask sets the \texttt{HttpOnly} flag on session cookies, preventing JavaScript access. However, the XSS vulnerability itself was confirmed — in a real scenario targeting an application without \texttt{HttpOnly}, session tokens would be exfiltrated.

% Screenshot placeholders
\begin{figure}[H]
  \centering
  \includegraphics[width=0.45\textwidth]{pictures/xss_cookies.png}
  \caption{XSS payload executing JavaScript alert in browser}
\end{figure}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/xss_source.png}
  \caption{Page source confirming lack of output encoding}
\end{figure}

\textbf{Impact Analysis}

\begin{warningbox}
\textbf{STRIDE Category:} Information Disclosure (T7) \\
\textbf{CVSS Score:} 6.1 Medium \\
\textbf{Impact:} An attacker can craft a malicious URL and send it to victims. When clicked, the script executes in the victim's browser and can steal cookies, redirect to phishing pages, or perform actions on behalf of the user.
\end{warningbox}

% ────────────────────────────────────────────────────────────────────────────
\subsection{Exercise 3 — OS Command Injection (T5)}
% ────────────────────────────────────────────────────────────────────────────

\textbf{Attack Description}

Command injection occurs when user input is passed unsanitized to a system shell command. Using \texttt{shell=True} in Python's \texttt{subprocess} module passes the entire string directly to the OS shell, allowing an attacker to append additional commands using shell operators.

\textbf{Target:} \texttt{GET /ping?host=}

\textbf{Windows vs WSL Context}

Initial testing was performed on Windows where Linux-specific commands were unavailable. The \texttt{ping} command on Windows uses \texttt{-n} instead of \texttt{-c}, and system files like \texttt{/etc/passwd} do not exist. The environment was migrated to WSL to replicate a realistic Linux server scenario.

On Windows, the pipe operator (\texttt{|}) was used to confirm command injection:

\begin{lstlisting}[style=urlstyle, caption={Windows command injection — whoami}]
http://127.0.0.1:5000/ping?host=127.0.0.1|whoami
\end{lstlisting}

The server returned: \texttt{desktop-v9jbra4\textbackslash electro-rimo}, confirming arbitrary command execution.

After migrating to WSL, Linux commands worked as intended:

\begin{lstlisting}[style=urlstyle, caption={WSL command injection — list home directory}]
http://127.0.0.1:5000/ping?host=127.0.0.1;ls+/home
\end{lstlisting}

\begin{lstlisting}[style=urlstyle, caption={WSL command injection — dump system users}]
http://127.0.0.1:5000/ping?host=127.0.0.1;cat+/etc/passwd
\end{lstlisting}

% Screenshot placeholders
\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/whoami_wind.png}
  \caption{Command injection confirmed on Windows — whoami executed}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.45\textwidth]{pictures/setup_env_wsl.png}
  \hfill
  \includegraphics[width=0.45\textwidth]{pictures/run_wsl.png}
  \caption{setup and run vulnerable app in WSL for Linux command testing}
\end{figure}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/ls_wsl.png}
  \caption{Home directory listing via command injection on WSL}
\end{figure}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/cat_passwd.png}
  \caption{System user file exposed via command injection}
\end{figure}

\textbf{Impact Analysis}

\begin{warningbox}
\textbf{STRIDE Category:} Denial of Service / Elevation of Privilege (T5) \\
\textbf{CVSS Score:} 9.8 Critical \\
\textbf{Impact:} An attacker gains the ability to execute arbitrary OS commands on the server. This can lead to full system compromise: reading sensitive files, creating backdoors, exfiltrating data, or crashing the server entirely.
\end{warningbox}

% ────────────────────────────────────────────────────────────────────────────
\subsection{Exercise 4 — Path Traversal (T6)}
% ────────────────────────────────────────────────────────────────────────────

\textbf{Attack Description}

Path traversal occurs when user-supplied filenames are passed directly to file system operations without validation. Using \texttt{../} sequences, an attacker can escape the intended directory and access arbitrary files on the server.

\textbf{Target:} \texttt{GET /read\_file?file=}

\textbf{Exploitation}

\begin{lstlisting}[style=urlstyle, caption={Path traversal — read system users}]
http://127.0.0.1:5000/read_file?file=../../etc/passwd
\end{lstlisting}

\begin{lstlisting}[style=urlstyle, caption={Path traversal — read application source code}]
http://127.0.0.1:5000/read_file?file=../app_vulnerable.py
\end{lstlisting}

Both requests returned the full file contents in the browser. Notably, absolute paths also worked because the vulnerable app uses Python's raw \texttt{open()} function:

\begin{lstlisting}[style=urlstyle, caption={Absolute path attack}]
http://127.0.0.1:5000/read_file?file=/etc/passwd
\end{lstlisting}

% Screenshot placeholders
\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/file_etc.png}
  \caption{System file exposed via path traversal}
\end{figure}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/app_vuln.png}
  \caption{Application source code exposed via path traversal}
\end{figure}

\textbf{Impact Analysis}

\begin{warningbox}
\textbf{STRIDE Category:} Elevation of Privilege (T6) \\
\textbf{CVSS Score:} 7.5 High \\
\textbf{Impact:} An attacker can read any file the application has permission to access, including configuration files, secret keys, SSL certificates, and the application source code itself. This information can be used to plan further attacks.
\end{warningbox}

% ════════════════════════════════════════════════════════════════════════════
\section{Secure Implementation}
% ════════════════════════════════════════════════════════════════════════════

After exploiting all vulnerabilities, \texttt{app\_secure.py} was examined to understand how each issue was remediated. The secure version follows a consistent principle: \textbf{never trust user input}.

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l X X}
\toprule
\textbf{\#} & \textbf{Vulnerability} & \textbf{Fix Applied} \\
\midrule
1 & SQL Injection & Parameterized queries using \texttt{?} placeholders \\
2 & XSS & Jinja2 templates with auto-escaping via \texttt{render\_template()} \\
3 & Command Injection & Subprocess list syntax + host whitelist; \texttt{shell=False} \\
4 & Path Traversal & \texttt{werkzeug.safe\_join()} + allowed filename whitelist \\
5 & Insecure Deserialization & Replace \texttt{pickle} with \texttt{json.get\_json()} + field whitelist \\
6 & Data Exposure & Remove passwords from API; require auth on all routes \\
7 & Hardcoded Secret & \texttt{os.environ.get('SECRET\_KEY')} or \texttt{secrets.token\_hex(32)} \\
8 & Debug Mode & \texttt{app.run(debug=False)}; use Gunicorn in production \\
\bottomrule
\end{tabularx}
\caption{Vulnerability fixes comparison}
\end{table}

\textbf{SQL Injection Fix:}
\begin{lstlisting}[style=pythonstyle, caption={Vulnerable vs Secure SQL query}]
# VULNERABLE
query = f"SELECT * FROM users WHERE username='{username}'"
conn.execute(query)

# SECURE
conn.execute("SELECT * FROM users WHERE username=?", (username,))
\end{lstlisting}

\textbf{Command Injection Fix:}
\begin{lstlisting}[style=pythonstyle, caption={Vulnerable vs Secure subprocess call}]
# VULNERABLE
output = subprocess.check_output(f"ping -c 1 {host}", shell=True)

# SECURE
ALLOWED_HOSTS = ["127.0.0.1", "localhost"]
if host not in ALLOWED_HOSTS:
    abort(403)
subprocess.check_output(["ping", "-c", "1", host])
\end{lstlisting}

\textbf{Path Traversal Fix:}
\begin{lstlisting}[style=pythonstyle, caption={Vulnerable vs Secure file access}]
# VULNERABLE
with open(filename) as f:
    return f.read()

# SECURE
ALLOWED_FILES = ["report.txt", "readme.txt"]
if filename not in ALLOWED_FILES:
    abort(403)
filepath = safe_join(BASE_DIR, filename)
with open(filepath) as f:
    return f.read()
\end{lstlisting}

% Screenshot placeholder
\begin{figure}[H]
  \centering
    \includegraphics[width=0.45\textwidth]{pictures/app_comparison.png}
  \caption{Side-by-side comparison of vulnerable and secure implementations}
\end{figure}

\textbf{Note on \texttt{safe\_join()} behavior:}

\texttt{werkzeug.safe\_join()} blocks both relative traversal (\texttt{../}) and absolute paths (\texttt{/etc/passwd}). The following table summarizes its behavior:

\begin{table}[H]
\centering
\begin{tabular}{l c c}
\toprule
\textbf{Payload} & \textbf{Vulnerable (open())} & \textbf{Secure (safe\_join())} \\
\midrule
\texttt{../../etc/passwd} & \textcolor{accentred}{\textbf{Works}} & \textcolor{codegreen}{\textbf{Blocked}} \\
\texttt{/etc/passwd} & \textcolor{accentred}{\textbf{Works}} & \textcolor{codegreen}{\textbf{Blocked}} \\
\texttt{../app\_vulnerable.py} & \textcolor{accentred}{\textbf{Works}} & \textcolor{codegreen}{\textbf{Blocked}} \\
\texttt{report.txt} (whitelisted) & \textcolor{accentred}{\textbf{Works}} & \textcolor{codegreen}{\textbf{Allowed}} \\
\bottomrule
\end{tabular}
\caption{safe\_join() behavior against different payloads}
\end{table}

% ════════════════════════════════════════════════════════════════════════════
\chapter{Part 2 — Test \& Automate}
% ════════════════════════════════════════════════════════════════════════════

In this part, automated security testing tools are integrated into the development workflow. The goal is to detect vulnerabilities automatically and block insecure code from being merged via a CI/CD pipeline. All tools were run on \textbf{WSL (Ubuntu)} with Python 3.13.12.

% ────────────────────────────────────────────────────────────────────────────
\section{Security Tools Overview}
% ────────────────────────────────────────────────────────────────────────────

Modern DevSecOps relies on a layered set of automated tools that run at different stages of the pipeline:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l X l}
\toprule
\textbf{Tool} & \textbf{Category} & \textbf{Purpose} & \textbf{Stage} \\
\midrule
Bandit & SAST & Finds Python code vulnerabilities & Pre-commit \\
Safety & SCA & Checks dependencies for CVEs & Pre-commit \\
pytest & Security Tests & Validates security behavior & CI \\
Gitleaks & Secret Scanning & Detects leaked credentials & Pre-commit \\
OWASP ZAP & DAST & Dynamic vulnerability scanning & Post-deploy \\
% Trivy & Container Scan & Scans Docker images for CVEs & CI \\
GitHub Actions & CI/CD & Orchestrates all security gates & Pipeline \\
\bottomrule
\end{tabularx}
\caption{Security tools used in Part 2}
\end{table}

% ────────────────────────────────────────────────────────────────────────────
\section{SAST — Static Application Security Testing with Bandit}
% ────────────────────────────────────────────────────────────────────────────

\textbf{What is SAST?}

Static Application Security Testing analyzes source code without executing it to find security vulnerabilities. Bandit is the standard Python SAST tool developed by PyCQA. It maps findings to CWE identifiers and severity levels.

\subsection{Installation and Commands}

\begin{lstlisting}[style=bashstyle, caption={Installing and running Bandit}]
pip install bandit

# Scan vulnerable app
bandit -r part1/app_vulnerable.py

# Scan secure app
bandit -r part1/app_secure.py
\end{lstlisting}

\subsection{Results on app\_vulnerable.py}

Bandit detected \textbf{8 issues} across the vulnerable application:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{Issue ID} & \textbf{Severity} & \textbf{CWE} & \textbf{Description} \\
\midrule
B602 & \textcolor{accentred}{\textbf{HIGH}} & CWE-78 & \texttt{subprocess} call with \texttt{shell=True} \\
B201 & \textcolor{accentred}{\textbf{HIGH}} & CWE-94 & Flask \texttt{debug=True} exposes Werkzeug debugger \\
B608 & \textcolor{warnorange}{\textbf{MEDIUM}} & CWE-89 & SQL query built via string interpolation \\
B301 & \textcolor{warnorange}{\textbf{MEDIUM}} & CWE-502 & \texttt{pickle.loads()} on untrusted data \\
B104 & \textcolor{warnorange}{\textbf{MEDIUM}} & CWE-605 & Binding to all interfaces (\texttt{0.0.0.0}) \\
B105 & Low & CWE-259 & Hardcoded password string in source \\
B404 & Low & CWE-78 & Import of subprocess module \\
B403 & Low & CWE-502 & Import of pickle module \\
\bottomrule
\end{tabularx}
\caption{Bandit findings on app\_vulnerable.py — 2 HIGH, 3 MEDIUM, 3 LOW}
\end{table}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.9\textwidth]{pictures/vuln_bandit.png}
  \caption{Bandit SAST scan on vulnerable application}
\end{figure}

\subsection{Results on app\_secure.py}

After remediation, only \textbf{3 LOW severity} informational warnings remained:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l l X}
\toprule
\textbf{Issue ID} & \textbf{Severity} & \textbf{CWE} & \textbf{Description} \\
\midrule
B404 & Low & CWE-78 & Import of subprocess (informational) \\
B607 & Low & CWE-78 & Process started with partial executable path \\
B603 & Low & CWE-78 & subprocess without \texttt{shell=True} (safe, informational) \\
\bottomrule
\end{tabularx}
\caption{Bandit findings on app\_secure.py — 0 HIGH, 0 MEDIUM, 3 LOW}
\end{table}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.9\textwidth]{pictures/secure_bandit.png}
  \caption{Bandit SAST scan on secure application}
\end{figure}

\subsection{Comparison Summary}

\begin{table}[H]
\centering
\begin{tabular}{l c c c}
\toprule
\textbf{Severity} & \textbf{app\_vulnerable.py} & \textbf{app\_secure.py} & \textbf{Reduction} \\
\midrule
HIGH   & 2 & 0 & \textcolor{codegreen}{\textbf{100\%}} \\
MEDIUM & 3 & 0 & \textcolor{codegreen}{\textbf{100\%}} \\
LOW    & 3 & 3 & 0\% (informational only) \\
\midrule
\textbf{Total} & \textbf{8} & \textbf{3} & \textcolor{codegreen}{\textbf{62.5\%}} \\
\bottomrule
\end{tabular}
\caption{Bandit results comparison}
\end{table}

\begin{infobox}
\textbf{Key finding:} The secure implementation eliminated 100\% of HIGH and MEDIUM severity findings. The 3 remaining LOW findings in \texttt{app\_secure.py} are informational — the subprocess usage is actually secure (list syntax, no \texttt{shell=True}).
\end{infobox}

% ────────────────────────────────────────────────────────────────────────────
\section{SCA — Software Composition Analysis with Safety}
% ────────────────────────────────────────────────────────────────────────────

\textbf{What is SCA?}

Software Composition Analysis checks third-party dependencies against known vulnerability databases (CVE, NVD). Unlike SAST which analyzes your code, SCA analyzes your \texttt{requirements.txt} to find vulnerable library versions.

\subsection{Installation Problem and Fix}

\begin{warningbox}
\textbf{Problem encountered:} The latest version of Safety was incompatible with Python 3.13 on WSL, throwing an \texttt{AttributeError: module 'typer' has no attribute 'rich\_utils'} error.\\[4pt]
\textbf{Fix:} Install a specific compatible version:
\begin{lstlisting}[style=bashstyle]
pip uninstall safety -y
pip install "safety==2.3.5"
\end{lstlisting}
\end{warningbox}

\subsection{Scan Results}

\begin{lstlisting}[style=bashstyle, caption={Running Safety SCA scan}]
safety check -r requirements.txt
\end{lstlisting}

Safety found \textbf{10 vulnerabilities} across 5 packages:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{Package} & \textbf{CVE} & \textbf{Description} \\
\midrule
flask 3.0.3 & CVE-2026-27205 & Information disclosure via missing cache headers \\
werkzeug 3.0.3 & CVE-2024-49766 & Path traversal on Windows — \texttt{safe\_join()} bypass \\
werkzeug 3.0.3 & CVE-2024-49767 & Resource exhaustion via multipart form parsing \\
werkzeug 3.0.3 & CVE-2026-27199 & DoS via Windows special device names \\
werkzeug 3.0.3 & CVE-2025-66221 & DoS via Windows device names before 3.1.4 \\
werkzeug 3.0.3 & CVE-2026-21860 & Improper handling of Windows reserved device names \\
gunicorn 22.0.0 & PVE-2024-72809 & HTTP request smuggling via conflicting headers \\
pytest 8.2.2 & CVE-2025-71176 & Local privilege escalation via predictable temp dir \\
requests 2.32.3 & CVE-2024-47081 & \texttt{.netrc} credential leak via malformed URLs \\
requests 2.32.3 & CVE-2026-25645 & Insecure temp file reuse in \texttt{extract\_zipped\_paths()} \\
\bottomrule
\end{tabularx}
\caption{Safety SCA scan — 10 vulnerabilities in 5 packages}
\end{table}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.9\textwidth]{pictures/safety_scan.png}
  \caption{Safety SCA scan results on requirements.txt}
\end{figure}

\subsection{Recommended Remediations}

\begin{table}[H]
\centering
\begin{tabular}{l l l}
\toprule
\textbf{Package} & \textbf{Current} & \textbf{Recommended Fix} \\
\midrule
flask     & 3.0.3  & Upgrade to \texttt{>=3.1.3} \\
werkzeug  & 3.0.3  & Upgrade to \texttt{>=3.1.6} \\
gunicorn  & 22.0.0 & Upgrade to \texttt{>=23.0.0} \\
pytest    & 8.2.2  & Upgrade to \texttt{>=9.0.3} \\
requests  & 2.32.3 & Upgrade to \texttt{>=2.33.0} \\
\bottomrule
\end{tabular}
\caption{Recommended dependency upgrades}
\end{table}

\begin{infobox}
\textbf{Note:} The werkzeug CVEs related to Windows path traversal are particularly relevant since this lab was initially run on Windows before migrating to WSL. Even \texttt{safe\_join()} — used as a fix in \texttt{app\_secure.py} — has known vulnerabilities in specific OS/version combinations, reinforcing the need for regular dependency scanning.
\end{infobox}

% ────────────────────────────────────────────────────────────────────────────
\section{Security Unit Testing with pytest}
% ────────────────────────────────────────────────────────────────────────────

\textbf{What are Security Tests?}

Security unit tests are automated assertions that verify security-relevant behavior — what the application should \textit{NOT} do. Unlike functional tests, they assert that attacks are blocked.

\subsection{Running the Test Suite}

\begin{warningbox}
\textbf{Problem encountered:} Running \texttt{pytest test\_security.py} from \texttt{part2/} failed with \texttt{ModuleNotFoundError: No module named 'app\_secure'} because \texttt{test\_security.py} imports from \texttt{app\_secure.py} which lives in \texttt{part1/}.\\[4pt]
\textbf{Fix:} Set the Python path explicitly:
\begin{lstlisting}[style=bashstyle]
cd part2
PYTHONPATH=../part1 pytest test_security.py -v
\end{lstlisting}
\end{warningbox}

\subsection{Test Results}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l l}
\toprule
\textbf{Test} & \textbf{Description} & \textbf{Result} \\
\midrule
\texttt{test\_valid\_login\_succeeds} & Valid credentials return 200 & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_invalid\_login\_fails} & Wrong password returns 401 & \textcolor{accentred}{\textbf{FAILED*}} \\
\texttt{test\_sql\_injection\_fails} & SQLi payload is rejected & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_invalid\_username\_format\_rejected} & Malformed username blocked & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_unauthenticated\_access\_redirects} & Protected routes redirect & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_xss\_payload\_escaped} & Script tags are HTML-escaped & \textcolor{accentred}{\textbf{FAILED*}} \\
\texttt{test\_xss\_href\_injection} & javascript: URI is blocked & \textcolor{accentred}{\textbf{FAILED*}} \\
\texttt{test\_api\_users\_requires\_auth} & API requires authentication & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_api\_users\_no\_passwords} & API never returns passwords & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_command\_injection\_blocked} & Injected commands are blocked & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_allowed\_host\_works} & Whitelisted hosts are allowed & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_json\_profile\_accepted} & Valid JSON profile accepted & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_unknown\_fields\_stripped} & Unknown fields are stripped & \textcolor{codegreen}{\textbf{PASSED}} \\
\texttt{test\_no\_server\_header\_leak} & Server header not exposed & \textcolor{codegreen}{\textbf{PASSED}} \\
\midrule
\textbf{Total} & & \textbf{11 passed / 3 failed} \\
\bottomrule
\end{tabularx}
\caption{Security test suite results}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/pytest.png}
  \caption{Security unit test results}
\end{figure}

\subsection{Analysis of Failures}

\begin{warningbox}
\textbf{Root cause of 3 failures:} \texttt{jinja2.exceptions.TemplateNotFound: login.html} and \texttt{search.html}\\[4pt]
The secure app correctly uses \texttt{render\_template()} instead of raw f-strings, but the \texttt{templates/} folder was not present when running tests from \texttt{part2/}. This is an \textbf{environment/path issue}, not a security regression.\\[4pt]
\textbf{Fix:} Copy the templates folder to the test directory:
\begin{lstlisting}[style=bashstyle]
cp -r part1/templates part2/templates
PYTHONPATH=../part1 pytest test_security.py -v
\end{lstlisting}
\textbf{Security impact:} None — the failures actually confirm that \texttt{render\_template()} is being used (the correct secure fix), rather than raw string responses.
\end{warningbox}

% ────────────────────────────────────────────────────────────────────────────
\section{Secret Scanning with Gitleaks}
% ────────────────────────────────────────────────────────────────────────────

\textbf{What is Secret Scanning?}

Gitleaks scans the entire git repository — including commit history — for accidentally leaked secrets like API keys, passwords, and tokens. Even deleted secrets remain in git history and can be found.

\subsection{Installation and Commands}

\begin{lstlisting}[style=bashstyle, caption={Installing and running Gitleaks}]
wget https://github.com/gitleaks/gitleaks/releases/download/v8.18.4/\
gitleaks_8.18.4_linux_x64.tar.gz
tar -xzf gitleaks_8.18.4_linux_x64.tar.gz

# Scan git history
./gitleaks detect --source . --verbose

# Scan files directly (no git required)
./gitleaks detect --source . --no-git --verbose
\end{lstlisting}

\subsection{Scan Results and False Positives}

\begin{warningbox}
\textbf{Problem encountered:} The initial scan with \texttt{--verbose} (without \texttt{--no-git}) returned \texttt{no leaks found} because \texttt{app\_vulnerable.py} had not been committed yet with the hardcoded secret.\\[4pt]
\textbf{Fix:} Run with full git history after committing:
\begin{lstlisting}[style=bashstyle]
./gitleaks detect --source . --verbose
\end{lstlisting}
\end{warningbox}

Gitleaks found \textbf{4 secrets} in the repository:

\begin{lstlisting}[style=bashstyle, caption={Gitleaks output}]
Finding: aws_secret="AKIAIMNOJVGFDXXXE4OA"
RuleID:  aws-access-token
File:    README.md  (Lines 221, 223, 259)

Finding: cafebabe:deadbeef
RuleID:  sidekiq-secret
File:    README.md  (Line 42)

4 commits scanned — leaks found: 4
\end{lstlisting}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/gitleaks.png}
  \caption{Gitleaks secret scanning results}
\end{figure}

\begin{infobox}
\textbf{False Positives:} All 4 findings are intentional example credentials in the Gitleaks \texttt{README.md} documentation file — not real secrets. This demonstrates that secret scanning tools require tuning to suppress known safe patterns.\\[4pt]
\textbf{Fix — create a \texttt{.gitleaks.toml} allowlist:}
\begin{lstlisting}[style=bashstyle]
cat > .gitleaks.toml << 'EOF'
[allowlist]
  description = "Allowlist for README examples"
  paths = ["README.md"]
EOF
\end{lstlisting}
This suppresses false positives while keeping detection active for real secrets.
\end{infobox}

% ────────────────────────────────────────────────────────────────────────────
\section{DAST — Dynamic Application Security Testing with OWASP ZAP}
% ────────────────────────────────────────────────────────────────────────────

\textbf{What is DAST?}

Dynamic Application Security Testing attacks the \textit{running} application automatically, just like a real attacker. Unlike SAST (reads code) and SCA (reads dependencies), ZAP sends real HTTP requests with malicious payloads and reports what works.

\subsection{Installation and Setup}

ZAP was run via Docker to avoid complex installation:

\begin{lstlisting}[style=bashstyle, caption={Pulling and running OWASP ZAP}]
docker pull ghcr.io/zaproxy/zaproxy:stable

# Run baseline scan against vulnerable app
docker run --rm ghcr.io/zaproxy/zaproxy:stable \
  zap-baseline.py \
  -t http://172.22.159.173:5000 \
  -l WARN
\end{lstlisting}

\begin{warningbox}
\textbf{Problem encountered:} ZAP running inside Docker could not reach the Flask app on \texttt{127.0.0.1} because that refers to the Docker container's own localhost, not the WSL host.\\[4pt]
\textbf{Fix:} Use the WSL IP address instead of \texttt{127.0.0.1}:
\begin{lstlisting}[style=bashstyle]
# Get WSL IP
hostname -I
# Use the returned IP (e.g. 172.22.159.173) in ZAP target URL
\end{lstlisting}
\textbf{Additional fix for secure app:} \texttt{app\_secure.py} binds to \texttt{127.0.0.1} by default. Temporarily change to \texttt{0.0.0.0} to allow Docker to reach it:
\begin{lstlisting}[style=bashstyle]
sed -i 's/host="127.0.0.1"/host="0.0.0.0"/' part1/app_secure.py
# Revert after scanning
sed -i 's/host="0.0.0.0"/host="127.0.0.1"/' part1/app_secure.py
\end{lstlisting}
\end{warningbox}

\subsection{Results — Vulnerable App (port 5000)}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{Finding} & \textbf{Risk} & \textbf{Attack Enabled} \\
\midrule
Missing Anti-clickjacking Header & Medium & Clickjacking via iframe embedding \\
X-Content-Type-Options Missing & Low & MIME sniffing attacks \\
Server Version Leak & Low & Reveals Werkzeug version for targeted exploits \\
CSP Header Not Set & Medium & No browser-level XSS defense \\
Permissions Policy Missing & Low & No control over browser features \\
CORP Header Missing & Low & Cross-origin resource access \\
\midrule
\textbf{Total} & & \textbf{6 warnings, 0 failures, 60 passes} \\
\bottomrule
\end{tabularx}
\caption{ZAP baseline scan on vulnerable app}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/zap_vuln.png}
  \caption{OWASP ZAP DAST scan on vulnerable application}
\end{figure}

\subsection{Results — Secure App (port 5001)}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{Finding} & \textbf{Risk} & \textbf{Notes} \\
\midrule
Missing Anti-clickjacking Header & Medium & Reduced from 4 to 2 instances \\
X-Content-Type-Options Missing & Low & Reduced from 4 to 2 instances \\
Server Version Leak & Low & Still present \\
CSP Header Not Set & Medium & To be fixed in Part 3 \\
Permissions Policy Missing & Low & To be fixed in Part 3 \\
CORP Header Missing & Low & To be fixed in Part 3 \\
Debug Error Messages & Info & \textbf{New:} /login returns 500 (missing templates) \\
Application Error Disclosure & Info & \textbf{New:} Same cause — environment issue \\
\midrule
\textbf{Total} & & \textbf{8 warnings, 0 failures, 58 passes} \\
\bottomrule
\end{tabularx}
\caption{ZAP baseline scan on secure app}
\end{table}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.9\textwidth]{pictures/zap_secure.png}
  \caption{OWASP ZAP DAST scan on secure application}
\end{figure}

\begin{infobox}
\textbf{Why did the secure app get more warnings?} The 2 new warnings (\texttt{Debug Error Messages} and \texttt{Application Error Disclosure}) were caused by the \texttt{/login} endpoint returning a 500 error due to the missing \texttt{login.html} template in the test environment — not a real security issue. In production with templates present, these warnings would disappear. The remaining header-related warnings are addressed in Part 3 via \texttt{security\_monitoring.py}.\\[4pt]
\textbf{Note on ZAP limitations:} The baseline scan (\texttt{zap-baseline.py}) performs passive analysis only. It did not detect SQL injection or XSS because those endpoints require authentication or specific parameters. A full active scan (\texttt{zap-full-scan.py}) would detect these but takes significantly longer.
\end{infobox}

% ────────────────────────────────────────────────────────────────────────────
\section{CI/CD Pipeline with GitHub Actions}
% ────────────────────────────────────────────────────────────────────────────

\textbf{What is a Security Gate?}

A security gate is an automated check in the CI/CD pipeline that blocks a pull request if a security condition is not met. This enforces that insecure code can never reach production.

\subsection{Pipeline Jobs Overview}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{l l X l}
\toprule
\textbf{Job} & \textbf{Tool} & \textbf{Purpose} & \textbf{Gate} \\
\midrule
SAST & Bandit & Scan Python code & Block if HIGH severity \\
SCA & Safety & Check dependencies & Block if critical CVE \\
Security Tests & pytest & Validate security behavior & Block if tests fail \\
Secret Scan & Gitleaks & Detect leaked credentials & Block if secret found \\
Container Scan & Trivy & Scan Docker image & Advisory only \\
DAST & OWASP ZAP & Dynamic scanning & Advisory only \\
Security Gate & Aggregator & Final merge check & Block if any job failed \\
\bottomrule
\end{tabularx}
\caption{CI/CD pipeline security gates}
\end{table}

\subsection{Problems Encountered and Fixes}

\begin{warningbox}
\textbf{Problem 1 — Invalid workflow syntax:}
The original \texttt{devsecops-pipeline.yml} used \texttt{hashFiles()} in a job-level \texttt{if:} condition, which is not supported by GitHub Actions:
\begin{lstlisting}[style=bashstyle]
# INVALID
if: hashFiles('Dockerfile') != ''

# FIX
if: true
\end{lstlisting}
\end{warningbox}

\begin{warningbox}
\textbf{Problem 2 — Wrong file paths:}
The original pipeline referenced \texttt{code/session1/} and \texttt{code/session2/} paths that do not exist in the cloned repository. All paths were updated to match the actual structure:
\begin{lstlisting}[style=bashstyle]
# ORIGINAL (wrong)
bandit -r code/session1/app_secure.py
safety check -r code/session1/requirements.txt
pytest code/session2/test_security.py

# FIXED
bandit -r app.py          # scans root app.py
safety check -r requirements.txt
PYTHONPATH=../part1 pytest part2/test_security.py
\end{lstlisting}
\end{warningbox}

\begin{warningbox}
\textbf{Problem 3 — Pipeline scanning \texttt{part1/app\_vulnerable.py}:}
Since the vulnerable app exists in \texttt{part1/}, Bandit would always find HIGH severity issues regardless of which branch was being tested.\\[4pt]
\textbf{Fix:} The pipeline was configured to scan only \texttt{app.py} at the repository root. Each branch places a different version there:
\begin{lstlisting}[style=bashstyle]
# insecure-app branch
cp part1/app_vulnerable.py app.py   # Pipeline FAILS

# main branch
cp part1/app_secure.py app.py       # Pipeline PASSES
\end{lstlisting}
\end{warningbox}

\subsection{Pipeline Execution Results}

\textbf{insecure-app branch — pipeline correctly fails:}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{pictures/vuln_CI.png}
  \caption{Pipeline failing on insecure-app branch}
\end{figure}

\textbf{main branch (secure app) — more jobs pass:}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.9\textwidth]{pictures/secure_CI.png}
  \caption{Pipeline on main branch with secure app}
\end{figure}

\textbf{Pull Request — security gate blocking merge:}

\begin{figure}[H]
  \centering
    \includegraphics[width=0.9\textwidth]{pictures/merge_blocked.png}
  \caption{Security gate blocking merge of insecure branch via Pull Request}
\end{figure}

\begin{infobox}
\textbf{Remaining pipeline failures on main branch:}\\
\textbf{Secret Scanning} still fails because the fake AWS credentials in \texttt{README.md} are detected by Gitleaks. These are intentional examples from the Gitleaks documentation. Fix: add a \texttt{.gitleaks.toml} allowlist file.\\[4pt]
\textbf{Container Security Scan} fails because the \texttt{Dockerfile} is in \texttt{part3/} not at the root. Fix: either move it or update the pipeline path.\\[4pt]
These are known infrastructure issues, not security vulnerabilities in the application.
\end{infobox}

\subsection{Key Principle — Shift-Left Security}

\begin{infobox}
The pipeline implements \textbf{shift-left security} — vulnerabilities are caught at commit time, before code reaches staging or production. Each security gate is a hard block: if Bandit finds a HIGH severity issue or Gitleaks finds a secret, the pull request cannot be merged until the issue is resolved. This transforms security from a final gate into a continuous practice embedded in every development cycle.
\end{infobox}
% ════════════════════════════════════════════════════════════════════════════
\chapter{Part 3 — Harden \& Monitor}
% ════════════════════════════════════════════════════════════════════════════

\begin{infobox}
\textbf{Note:} This chapter will be completed after finishing Part 3 of the lab. Sections covered: Docker hardening, WAF, security headers, and incident response.
\end{infobox}

\section{Docker Security Hardening}
% Content to be added after completing Part 3

\section{Runtime Security Monitoring}
% Content to be added after completing Part 3

\section{Security Headers}
% Content to be added after completing Part 3

\section{Incident Response Exercise}
% Content to be added after completing Part 3

% ════════════════════════════════════════════════════════════════════════════
\chapter{Conclusion}
% ════════════════════════════════════════════════════════════════════════════

\begin{infobox}
\textbf{Note:} Conclusion to be written after completing all three parts of the lab.
\end{infobox}

% ════════════════════════════════════════════════════════════════════════════
\chapter*{References}
\addcontentsline{toc}{chapter}{References}
% ════════════════════════════════════════════════════════════════════════════

\begin{enumerate}
  \item OWASP Top Ten. \textit{OWASP Foundation}. \url{https://owasp.org/www-project-top-ten/}
  \item STRIDE Threat Modeling. \textit{Microsoft Security}. \url{https://learn.microsoft.com/en-us/azure/security/develop/threat-modeling-tool-threats}
  \item Bandit Documentation. \textit{PyCQA}. \url{https://bandit.readthedocs.io/}
  \item Safety Documentation. \url{https://pyup.io/safety/}
  \item CIS Docker Benchmark v1.6. \textit{Center for Internet Security}.
  \item Werkzeug Documentation — safe\_join. \url{https://werkzeug.palletsprojects.com/}
  \item Flask Security Documentation. \url{https://flask.palletsprojects.com/en/latest/security/}
  \item Lab Repository. \url{https://github.com/said-dev-ops/dev-sec-ops-lab-1}
\end{enumerate}

\end{document}
